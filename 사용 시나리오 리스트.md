

### 📂 확장자 및 파일 유형별 자동 분류

- "다운로드 폴더에 있는 모든 `.pdf`, `.docx`, `.hwp` 파일들을 문서 폴더로, `.zip`, `.tar` 파일들은 압축파일 폴더로 각각 자동 분류해줘."
    
- "외주 작업 폴더에서 영상 원본 파일(`.mp4`, `.mov`)과 자막 파일(`.srt`)을 매칭해서 같은 이름의 하위 폴더를 만들어 각각 넣어줘."
    
- "바탕화면에 있는 오디오 파일(`.mp3`, `.wav`)만 골라내서 음악 폴더 안의 `[미분류_음원]`으로 이동시켜줘."
    

### 📅 날짜 및 생성 시점 기준 정렬

- "`2026` 폴더 안에 있는 파일들을 각 파일이 '수정된 날짜' 기준으로 `1월`, `2월`, `3월` 등 월별 폴더를 새로 만들어서 분할 배치해줘."
    
- "최근 일주일 동안 생성된 스크린샷 파일만 찾아서 바탕화면에 `주간_스크랩` 폴더를 만들고 그리로 옮겨줘."
    
- "생성된 지 3년이 지난 백업 폴더 내용물 중 중요 표시(`*`)가 없는 파일들은 전부 휴지통으로 보내줘."
    

### 🔍 텍스트 내용 및 메타데이터 기반 필터링

- "내 문서 폴더의 수많은 텍스트 파일과 엑셀 파일 중, 본문에 '계약서' 또는 '인보이스'라는 단어가 포함된 파일만 필터링해서 `법무_검토` 폴더로 복사해줘."
    
- "사진 폴더에서 메타데이터(Exif)를 확인하고, 촬영 장소가 '제주도'로 되어 있는 사진만 골라 `2025_제주여행`폴더로 모아줘."
    
- "소스코드 폴더(`.py`, `.js`) 중에서 주석(`//` 또는 `#`)을 제외한 실제 코드 라인이 1,000줄이 넘는 파일만 찾아 목록을 텍스트 파일로 저장해줘."
    

### 🧹 중복 및 정크 파일 정리 (용량 확보)

- "이름은 다르지만 파일 크기와 데이터 내용(해시값)이 완전히 일치하는 중복 이미지 파일들을 다 찾아서 최신 것 하나만 남기고 나머지는 삭제해줘."
    
- "시스템 임시 폴더(`Temp`)와 브라우저 캐시 폴더를 열어서 100MB 이상 차지하는 정크 파일들을 안전하게 비워줘."
    
- "프로그램을 삭제한 뒤에도 `AppData` 폴더에 남아 있는 빈 폴더나 잔재 폴더들을 찾아서 깔끔하게 정리해줘."
    

### 🏷️ 파일 이름 일괄 변경 (Batch Rename)

- "`C:\Project\Images` 폴더에 있는 이미지 파일 50개의 이름을 촬영 날짜 순서대로 `Img_001.jpg`, `Img_002.jpg`와 같이 일련번호 형태로 일괄 변경해줘."
    
- "파일명에 공백(띄어쓰기)이나 특수문자가 들어간 파일들을 찾아서 공백을 언더바(`_`)로 모두 바꿔줘."
    
- "기존 파일명 앞에 오늘 날짜인 `20260609_`를 접두사로 전부 붙여줘."
    

### 🔒 보안 및 백업 관리

- "바탕화면에 있는 '개인정보' 관련 엑셀 파일들을 암호화된 압축 파일(`.zip`)로 만들고, 원본 파일은 복구가 불가능하도록 영구 삭제(Shred)해줘."
    
- "매일 오후 6시마다 `작업_진행중` 폴더의 전체 내용을 외장 하드 드라이브(`D:\Backup`)로 동기화(미러링)하는 작업을 예약해줘."
    

