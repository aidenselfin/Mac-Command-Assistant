# 구현 계획서: Voice-Action AI v1.0

> PRD 기준 날짜: 2026-06-09  
> 목표: R-cmd 누름 → 음성 명령 → 파일 정리 완료까지 10초 이내

---

## 프로젝트 구조

```
voice-action/
├── main.py                  # 진입점: 핫키 루프
├── audio.py                 # sounddevice 녹음 + WAV 변환
├── stt.py                   # faster-whisper STT
├── scanner.py               # 파일 스캔 + .fs.snap 생성 + 캐시
├── llm.py                   # Claude Haiku 호출 + JSON 파싱
├── executor.py              # 액션 실행 + 롤백 + 로그
├── ui/
│   ├── preview.py           # 미리보기 패널 (tkinter)
│   └── onboarding.py        # 권한 확인 + API 키 설정 패널
├── config.py                # ~/.voice-action/config.json 읽기/쓰기
├── permissions.py           # 마이크·디스크·손쉬운 사용 권한 확인
└── requirements.txt
```

---

## 구현 단계

### Phase 1 — 핵심 파이프라인 (의존성 없는 백엔드)

**목표**: UI 없이 CLI로 전체 파이프라인 동작 확인  
**검증**: `python main.py --test "다운로드 폴더 PDF Documents로 이동"` 실행 후 액션 플랜 JSON 출력

#### 1-1. `scanner.py`

```
scan_directory(root, include_hidden) -> str   # .fs.snap 텍스트 반환
get_snap(root) -> str                         # mtime 캐시 적용
_sha256_prefix(p) -> str                      # SHA256 앞 8자리
_resolve_dst_conflict(dst) -> Path            # _1, _2 접미사
```

- MAX_FILES=500 초과 시 mtime 최신순 정렬 후 슬라이싱
- EXCLUDE_DIRS: `.git`, `node_modules`, `.Trash`, `__pycache__`
- SnapCache dataclass로 mtime dict 비교

**검증**: `~/Documents`, `~/Downloads`, `~/Desktop` 3곳 스캔 후 `.fs.snap` 포맷 출력 확인

#### 1-2. `llm.py`

```
call_llm(voice_text, fs_snap) -> list[dict]   # JSON 액션 플랜
```

- 시스템 프롬프트 전체에 `cache_control: {"type": "ephemeral"}` 적용
- `json.loads()` 실패 시 정규식 `\[.*\]` 재추출
- `action` 값이 `move/rename/delete` 외이면 해당 항목만 제거

**검증**: 실제 `.fs.snap` + 샘플 명령으로 API 호출 → JSON 배열 반환 확인

#### 1-3. `executor.py`

```
execute_plan(actions, log_path) -> list[dict]  # 실행 결과 로그
_move_to_trash(p) -> None                      # ~/.Trash 직접 이동
```

- `delete`는 `~/.Trash` 직접 이동 (osascript 미사용)
- 로그: `~/.voice-action/logs/<ISO_TIMESTAMP>.json`

**검증**: 테스트 파일 3개 move/delete 실행 → 로그 파일 생성 확인

---

### Phase 2 — 입력 레이어 (오디오 + STT)

**목표**: R-cmd hold → 녹음 → 텍스트 변환

#### 2-1. `audio.py`

```
AudioRecorder                                 # 클래스
  .start()                                    # sounddevice 스트림 시작
  .stop() -> bytes                            # WAV bytes 반환
```

- `sounddevice.InputStream`으로 numpy array accumulate
- 종료 시 `scipy.io.wavfile.write` 또는 `wave` 모듈로 WAV 변환

**검증**: 3초 녹음 후 WAV 파일 재생 확인

#### 2-2. `stt.py`

```
transcribe(wav_bytes) -> str                  # 한국어 텍스트
```

- `faster-whisper` small 모델, `language="ko"`
- 결과가 빈 문자열이면 `SpeechRecognitionError` 발생

**검증**: "다운로드 폴더 정리해줘" 발화 → 텍스트 정확도 확인

#### 2-3. `main.py` — 핫키 연결

```python
from pynput import keyboard

def on_press(key):
    if key == keyboard.Key.cmd_r:
        recorder.start()

def on_release(key):
    if key == keyboard.Key.cmd_r:
        wav = recorder.stop()
        asyncio.run(pipeline(wav))
```

- 핫키 리스너는 별도 스레드에서 실행
- `pipeline()`: STT + 선스캔 `asyncio.gather` 병렬 → LLM → 미리보기

**검증**: R-cmd hold/release → STT 텍스트 터미널 출력 확인

---

### Phase 3 — UI 레이어

**목표**: 미리보기 패널 표시 → 사용자 확인 → 실행

#### 3-1. `ui/preview.py`

```
show_preview(actions) -> bool    # True=실행, False=취소
```

- tkinter Toplevel, 별도 스레드에서 실행
- 액션 목록 렌더링:
  - `move` / `rename`: 일반 텍스트
  - `delete`: 빨간색 강조 + 경고 문구
- delete 항목 있을 때 [실행] 버튼만 활성화 (confirm gate)
- ESC / 외부 클릭 → 취소

#### 3-2. `ui/onboarding.py`

```
show_onboarding(permissions)     # 권한 부족 시 표시
```

- 권한별 체크마크 + [시스템 설정→] 버튼
- API 키 미설정 시 입력 필드 추가

---

### Phase 4 — 설정 + 권한

#### 4-1. `config.py`

- `~/.voice-action/config.json` 읽기/쓰기
- 환경변수 `ANTHROPIC_API_KEY` 우선 적용
- 기본값: `default_scan_dirs=["~/Documents", "~/Downloads", "~/Desktop"]`, `whisper_model=small`

#### 4-2. `permissions.py`

```
check_permissions() -> dict[str, bool]
  # microphone: AVCaptureDevice
  # full_disk: ~/Library/Application Support 쓰기 테스트
  # accessibility: AXIsProcessTrusted()
```

- 앱 시작 시 호출, 하나라도 False면 onboarding 패널 표시 + 기능 비활성화

---

## 의존성 설치 순서

```
pip install faster-whisper sounddevice numpy anthropic pynput pyobjc-framework-Cocoa pyobjc-framework-AVFoundation
```

> `rumps`는 macOS 전용. `pyobjc`는 권한 확인에 필요.

---

## 단계별 검증 기준

| Phase | 검증 방법 | 성공 기준 |
|-------|----------|----------|
| 1-1 scanner | `python -c "from scanner import get_snap; print(get_snap(Path('~/Documents').expanduser()))"` | .fs.snap 포맷 출력 |
| 1-2 llm | 샘플 snap + 명령으로 `call_llm()` 호출 | JSON 배열 반환, $0.001 미만 비용 |
| 1-3 executor | 테스트 디렉토리에서 move/delete 실행 | 로그 파일 생성 확인 |
| 2-1 audio | 3초 녹음 WAV 저장 | 재생 가능한 WAV |
| 2-2 stt | 한국어 문장 발화 | 80% 이상 인식 정확도 |
| 2-3 hotkey | R-cmd 누름/뗌 | 터미널에 STT 텍스트 출력 |
| 3 UI | 전체 플로우 수동 실행 | 미리보기 → 실행 → 완료 배너 → 롤백 동작 |

---

## 구현 시 주의사항

- **tkinter 스레드 분리**: tkinter는 별도 스레드에서 실행 — 핫키 루프 스레드에서 직접 호출 금지
- **delete는 Trash만**: `os.unlink()` 직접 호출 금지, 반드시 `_move_to_trash()` 경유
- **subprocess 환경 격리**: `subprocess.run()` 시 `env={}` 전달로 API 키 노출 방지
- **STT + 선스캔 병렬**: `asyncio.gather(transcribe_task, scan_task)` — 항상 3곳(Documents/Downloads/Desktop) 동시 스캔, 폴더 힌트 감지 없음
- **Prompt Caching TTL**: 5분 — 연속 명령 세션에서 자동 적용, 별도 처리 불필요

---

## 성공 기준 (PRD)

| 항목                 | 목표          |
| ------------------ | ----------- |
| 전체 지연 (R-cmd → 완료) | 5초 이내       |
| LLM 호출 횟수          | 명령당 1회      |
| 파일 이동 정확도          | 90% 이상      |



