# 📊 PRD: Data-Driven Smart Shortcut Optimizer (가칭: Track & Row)

## 1. 시스템 필요 요건 및 구성 요소 (Requirements)
이 프로그램이 정상적으로 작동하기 위해 구축해야 하는 4가지 핵심 모듈입니다.

* **이벤트 감지 모듈 (Event Listener):** 사용자의 마우스 클릭, 키보드 입력, 그리고 현재 활성화된 앱의 전환을 백그라운드에서 감지합니다.
* **UI 요소 추출기 (Context Extractor):** Mac의 손쉬운 사용(Accessibility API)을 호출하여, 이벤트가 발생한 지점의 '앱 이름'과 '버튼(요소) 이름', '계층적 창 정보'를 가져옵니다.
* **로컬 저장소 및 분석 엔진 (Local DB & Analyzer):** 수집된 텍스트 로그(CSV)를 저장하고, 앱별로 가장 많이 사용된 버튼의 순위와 가중치를 계산합니다. 또한 주요 앱들의 기본 단축키 데이터베이스(`PHASE3/shortcuts_db.json`)를 연동하여 학습의 기준 데이터를 제공합니다.
* **오버레이 UI (Overlay Interface):** Mac 네이티브(PyObjC) 기반의 투명 윈도우 창입니다. 창이나 내부 패널을 인식하여 하이라이트(테두리, 태그 등)를 띄워주거나, 마우스 클릭 시점에 해당 기능의 단축키를 화면에 즉각적으로(1.5초간) 띄워 시각적 힌트를 제공합니다.

## 2. 반드시 만족해야 하는 핵심 조건 (Success Conditions)
프로그램이 실사용 가능하려면 다음 조건들을 기술적으로 반드시 클리어해야 합니다.

* **이벤트 구동 및 스캔 최적화 (Event-Driven & Dual-Tier Scanning):** 화면을 실시간으로 무겁게 스캔하여 CPU와 배터리를 소모하면 안 됩니다. '마우스를 클릭하는 순간' 또는 '단축키를 누른 순간'에만 작동하며, 단일 창(모드 1)과 내부 세부 패널(모드 2)을 구분하여 스캔 깊이를 조절해야 합니다.
* **절대적 로컬 보안:** 마우스와 키보드 움직임을 추적하므로, 모든 데이터(CSV, JSON DB 등)는 로컬 환경에만 존재해야 하며 서버 전송이 없어야 합니다.
* **맥락 기반 데이터 수집 (Stable Identification):** 창 크기가 변하거나 재시작해도 추적할 수 있도록, 단순 좌표가 아닌 Accessibility 속성 기반의 패널 식별명(예: sidebar, terminal)과 UI Role을 사용하여 데이터를 저장해야 합니다.
* **학습 유도 및 피드백 루프 (Muscle Memory):** 키보드 사용을 유도하기 위해 마우스 클릭 시점에 해당하는 단축키 힌트를 바로 보여주어야 합니다.

## 3. 프로그램 개발 및 실행 로직 (Architecture & Flow)
프로그램은 백그라운드에서 다음과 같은 흐름으로 사용자의 키보드 중심 워크플로우를 유도합니다.

1. **데이터 수집 (Background Logging):**
   * 사용자가 특정 앱에서 버튼이나 텍스트 박스 등을 마우스로 클릭합니다.
   * 클릭 위치와 앱 정보를 로컬 CSV에 기록합니다.
2. **분석 및 매핑 (Local Shortcut Matching):**
   * 로컬 단축키 DB(`PHASE3/shortcuts_db.json`)를 사용하여 클릭한 UI 요소에 매칭되는 단축키를 찾습니다.
   * 매칭된 경우 단축키 힌트를 즉시 사용자에게 제공합니다.
3. **단축키 할당 및 UI 표출 (Tagging & Hinting):**
   * **오버레이 태그:** 현재 화면에 보이는 창/패널을 알파벳 태그로 시각화합니다.
   * **단축키 힌트:** 마우스 클릭 시 해당 요소에 대한 단축키가 존재하면 1.5초간 임시 태그로 표시합니다.
4. **키보드 포커스 이동 (Navigation):**
   * `Right Command + [알파벳]` 조합을 통해 선택한 창/패널로 포커스를 이동합니다.

## 4. 현재 구현 현황 (Current Status)

- `PHASE1/logger.py`를 통해 마우스 클릭 및 앱 전환 로그를 로컬에 저장하는 데이터 수집 기능이 구현되었습니다.
- `PHASE3/overlay_engine.py`에서 화면의 창/패널을 실시간 스캔하고 알파벳 태그를 표시하는 오버레이 엔진이 구현되었습니다.
- `Right Command` 기반 입력으로 알파벳 태그를 선택하면 해당 창/패널로 포커스 이동이 동작합니다.
- 마우스 클릭 시 `PHASE3/shortcuts_db.json`에서 단축키를 찾아 화면에 임시 힌트를 표시하는 학습 유도 기능이 구현되었습니다.

## 5. 문서 구성

- `README.md`: 프로젝트 개요와 현재 구현 상태
- `PRD.md`: 제품 요구사항 및 현재 상태
- `dev_plan.md`: 전체 개발 계획 (수정 금지)
- `PHASE1/PHASE1_DEV_PLAN.md`: Phase 1 데이터 로거 계획
