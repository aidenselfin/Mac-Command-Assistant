# 📊 PRD: Data-Driven Smart Shortcut Optimizer (가칭: Track & Row)

## 1. 시스템 필요 요건 및 구성 요소 (Requirements)
이 프로그램이 정상적으로 작동하기 위해 구축해야 하는 4가지 핵심 모듈입니다.

* **이벤트 감지 모듈 (Event Listener):** 사용자의 마우스 클릭, 키보드 입력, 그리고 현재 활성화된 앱의 전환을 백그라운드에서 감지합니다.
* **UI 요소 추출기 (Context Extractor):** Mac의 손쉬운 사용(Accessibility API)을 호출하여, 이벤트가 발생한 지점의 '앱 이름'과 '버튼(요소) 이름', '계층적 창 정보'를 가져옵니다. 성능을 위해 빠른 화면 캡처(Quartz)와 깊은 UI 탐색(AX Tree 스캔)을 혼용합니다.
* **로컬 저장소 및 분석 엔진 (Local DB & Analyzer):** 수집된 텍스트 로그(CSV)를 저장하고, 앱별로 가장 많이 사용된 버튼의 순위와 가중치를 계산합니다. 또한 주요 앱들의 기본 단축키 데이터베이스(`shortcuts_db.json`)를 연동하여 학습의 기준 데이터를 제공합니다.
* **오버레이 UI (Overlay Interface):** Mac 네이티브(PyObjC) 기반의 투명 윈도우 창입니다. 창이나 내부 패널을 인식하여 하이라이트(테두리, 태그 등)를 띄워주거나, 마우스 클릭 시점에 해당 기능의 단축키를 화면에 즉각적으로(예: 1.5초) 띄워 시각적 힌트를 제공합니다.

## 2. 반드시 만족해야 하는 핵심 조건 (Success Conditions)
프로그램이 실사용 가능하려면 다음 조건들을 기술적으로 반드시 클리어해야 합니다.

* **이벤트 구동 및 스캔 최적화 (Event-Driven & Dual-Tier Scanning):** 화면을 실시간으로 무겁게 스캔하여 CPU와 배터리를 소모하면 안 됩니다. '마우스를 클릭하는 순간' 또는 '단축키를 누른 순간'에만 작동하며, 단일 창(모드 1)과 내부 세부 패널(모드 2)을 구분하여 스캔 깊이를 조절해야 합니다.
* **절대적 로컬 보안:** 마우스와 키보드 움직임을 추적하므로, 모든 데이터(CSV, JSON DB 등)는 로컬 환경에만 존재해야 하며 서버 전송이 없어야 합니다.
* **맥락 기반 데이터 수집 (Stable Identification):** 창 크기가 변하거나 재시작해도 추적할 수 있도록, 단순 좌표가 아닌 Accessibility 속성 기반의 패널 식별명(예: sidebar, terminal)과 UI Role을 사용하여 데이터를 저장해야 합니다.
* **학습 유도 및 피드백 루프 (Muscle Memory):** 키보드 사용을 유도하기 위해 마우스 클릭 시점에 해당하는 단축키 힌트를 바로 보여주어야 합니다. 만약 단축키를 알려주었는데도 마우스를 고집한다면 가중치를 조정하고, 숙달된 단축키는 점차 시각적 노출을 줄이는(Fade-out) 피드백이 적용되어야 합니다.

## 3. 프로그램 개발 및 실행 로직 (Architecture & Flow)
프로그램은 백그라운드에서 다음과 같은 흐름으로 사용자의 키보드 중심 워크플로우를 유도합니다.

1. **데이터 수집 (Background Logging):**
   * 사용자가 특정 앱에서 버튼이나 텍스트 박스 등을 마우스로 클릭합니다.
   * `AXUIElementCopyElementAtPosition` 등을 통해 해당 위치의 구체적인 UI 요소를 즉시 추출하고, 지능적으로 의도를 분류하여 CSV 로그로 기록합니다.
2. **분석 및 최적화 (Data Analysis):**
   * 누적된 데이터를 바탕으로 사용자가 자주 마우스로 클릭하는 기능들을 추출합니다.
   * 이 정보와 로컬 단축키 DB(`shortcuts_db.json`)를 대조하여 단축키 매핑 여부를 판단합니다.
3. **단축키 할당 및 UI 표출 (Tagging & Hinting):**
   * **네비게이션 오버레이:** 전역 단축키(예: `Cmd + Shift + K`) 입력 시 모드 1(Windowed) 또는 모드 2(Full-Screen)에 맞춰 화면의 주요 창과 서브 패널을 식별하고 시각적으로 포커싱/태깅합니다.
   * **실시간 단축키 힌트:** 사용자가 마우스 클릭을 했을 때 DB에 매칭되는 단축키가 있다면 클릭 위치에 1.5초간 힌트(예: `Cmd + E`)를 렌더링해 즉각적인 학습을 유도합니다.
4. **학습 및 적응 (Feedback Loop):**
   * 사용자가 제시된 단축키를 활용하여 키보드로 조작하면 해당 항목의 숙련도 점수를 올리고, 오버레이 UI의 투명도를 점진적으로 높여(안 보이게) 시각적 피로를 줄입니다.

## 4. 단계별 개발 계획 (Milestones)
전체 시스템의 최종 목표(Phase 1~4)를 유지하면서, 현재 구체화 및 완료된 기술적 사항을 반영한 로드맵입니다.

* **Phase 1: 데이터 로거 구축 (수집 중심) - [기본 구현 완료]**
   * 파이썬 백그라운드 스크립트로 마우스/트랙패드 클릭 등 이벤트 감지.
   * Mac Accessibility API를 활용하여 클릭 지점의 앱 이름, UI 요소 Role 및 Title을 안정적으로 추출 (UNKNOWN_ELEMENT 최소화 적용).
   * 추출된 데이터를 로컬 `.csv` 파일에 타임스탬프와 함께 기록.
* **Phase 2: 데이터 분석기 개발 (추천 중심) - [진행 예정]**
   * 누적된 `.csv` 데이터를 분석해 앱별 최다 마우스 클릭 기능 도출 (Pandas 등 활용).
   * 최근 많이 사용된 기능에 가중치를 주는 EMA(지수 이동 평균) 기반 스코어링 도입.
* **Phase 3: 적응형 오버레이 엔진 고도화 (시각화 및 힌트) - [구현 및 고도화 완료]**
   * PyObjC 기반의 스레드 세이프(Thread-safe)한 네이티브 투명 윈도우 렌더링.
   * **계층적 UI 스캔:** 단일 창(모드 1)과 내부 스플릿 뷰(모드 2)를 구분하는 Dual-Tier 스캔 방식 적용으로 성능 최적화.
   * 안정적인 패널 식별 (사이드바, 터미널, 코드 에디터, 텍스트 박스 등 분리).
   * **단축키 학습 힌트:** `shortcuts_db.json` 연동을 통해 클릭한 UI 요소의 단축키를 찾아 1.5초간 화면에 임시 태그로 띄우는 학습 유도 기능 탑재.
* **Phase 4: 피드백 반영 및 근육 기억 유도 (완성) - [진행 예정]**
   * 키보드 조작 여부와 마우스 클릭을 교차 검증하여 네거티브/포지티브 피드백 반영.
   * 단축키 사용 숙련도에 따라 힌트 및 태그의 투명도(Opacity) 속성을 자동 조절하는 맞춤형 최적화 기능 완성.