# requirements.txt — Smart-Homerow Dependencies
# 설치: pip install -r requirements.txt

# 이벤트 감지 (PHASE1)
pynput>=1.7.6

# macOS 네이티브 API (PyObjC)
PyObjC>=9.0

# 유틸리티 (Cocoa 번들에 AppKit·Foundation 포함 — 별도 AppKit 패키지는 PyPI에 없음)
pyobjc-framework-Cocoa>=9.0
pyobjc-framework-ApplicationServices>=9.0
pyobjc-framework-Quartz>=9.0
# CoreFoundation은 pyobjc-core(위 패키지들의 의존성)에 포함 — 별도 PyPI 패키지 없음

# 데이터베이스 (PHASE2)
# sqlite3는 Python 표준 라이브러리에 포함됨

# 오버레이 UI (PHASE3)
# AppKit은 PyObjC에 포함됨
